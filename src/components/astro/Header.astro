<header id="main-header">
  <slot />
</header>

<style>
  header {
    background-color: var(--color-primary-dark-purple-50);
    border-bottom: 1px solid var(--color-surface-light);
    position: sticky;
    top: 0;
    width: 100%;
    height: calc(var(--size-height-header) + var(--size-width-border-sm));
    z-index: var(--layer-top);
  }
</style>
